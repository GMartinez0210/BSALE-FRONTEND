/**
 * It returns a string of HTML code that contains a heading with a class of "suggestion" and the text
 * "Lo invitamos a elegir otro producto :)"
 * @returns A string.
 */
export function nothing() {
    return `<h3 class="suggestion">Lo invitamos a elegir otro producto :) </h3>`
}